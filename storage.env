# .env.example — Desarrollo actual (disco 500GB local)
STORAGE_ROOT=./data
IMG_OPTIMIZATION_ENABLED=true
IMG_MAX_DIMENSION=1920
IMG_JPEG_QUALITY=80

# .env — Producción futura (RAID 2x2TB montado en /mnt/storage)
# STORAGE_ROOT=/mnt/storage/ssei_data